{
    "name": "pulumi-azure-native-splitter",
    "description": "Splits @pulumi/azure-native into smaller packages",
    "scripts": {
        "build": "ts-node src/index.ts build",
        "create-types": "ts-node src/index.ts create-types",
        "publish": "ts-node src/index.ts publish",
        "publish:dryrun": "ts-node src/index.ts publish --dry-run",
        "unpublish": "ts-node src/index.ts unpublish"
    },
    "devDependencies": {
        "@types/node": "^20.5.9",
        "commander": "^11.0.0",
        "ts-node": "^10.9.1",
        "typescript": "^5.2.2"
    }
}
