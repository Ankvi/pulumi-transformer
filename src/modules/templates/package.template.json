{
    "name": "${PACKAGE_NAME}",
    "version": "${VERSION}",
    "description": "Pulumi package for ${NAME}",
    "keywords": ["pulumi", "azure", "azure-native", "category/cloud", "kind/native"],
    "homepage": "https://pulumi.com",
    "repository": {
        "url": "git://github.com/Ankvi/pulumi-transformer.git",
        "type": "git"
    },
    "dependencies": {
        "@pulumi/pulumi": "^3.0.0"
    },
    "scripts": {
        "install": "node scripts/install-pulumi-plugin.js resource azure-native ${VERSION}"
    }
}
