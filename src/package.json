{
    "name": "cli",
    "description": "Splits @pulumi/azure-native into smaller packages",
    "scripts": {
        "build": "ts-node index.ts build",
        "create-types": "ts-node index.ts create-types",
        "publish": "ts-node index.ts publish",
        "publish:dryrun": "ts-node index.ts publish --dry-run",
        "unpublish": "ts-node index.ts unpublish"
    },
    "devDependencies": {
        "@types/node": "^20.5.9",
        "commander": "^11.0.0",
        "ts-node": "^10.9.1",
        "typescript": "^5.2.2"
    },
    "dependencies": {
        "octokit": "^3.1.0"
    }
}
