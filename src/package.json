{
    "name": "cli",
    "description": "Splits @pulumi/azure-native into smaller packages",
    "scripts": {
        "create-output": "ts-node index.ts build",
        "publish": "ts-node index.ts publish",
        "publish:dryrun": "ts-node index.ts publish --dry-run",
        "unpublish": "ts-node index.ts unpublish",
        "list-module-names": "ts-node index.ts list-module-names"
    },
    "devDependencies": {
        "@types/node": "^20.5.9",
        "ts-node": "^10.9.1",
        "typescript": "^5.2.2"
    },
    "dependencies": {
        "commander": "^11.0.0",
        "@octokit/rest": "^20.0.1"
    }
}
